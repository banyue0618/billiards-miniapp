# 自助台球厅系统详细设计文档

## 文档说明

### 1.版本记录

| 版本号 | 修订日期   | 修订说明                                           |
| ------ | ---------- | -------------------------------------------------- |
| V1.0   | -          | 初始版本                                           |
| V2.0   | 2023-02-15 | 完善数据库设计和API接口设计                        |
| V2.1   | 2023-03-10 | 添加计价模块详细设计                               |
| V2.2   | 2023-04-20 | 补充仪表盘设计、API接口和未来规划                  |

### 2.功能实现状态说明

- ✅ 已实现：功能已完整实现
- ⚠️ 部分实现：功能已部分实现，待完善
- ❌ 未实现：功能尚未实现
- 💡 建议功能：非必要但具有商业价值的功能建议
- 🔄 待优化：已实现但需要优化的功能
- ⭐ 核心功能：系统核心业务功能
- 🔒 安全敏感：需要特别注意安全性的功能

1. 

## 1. 系统概述

### 1.1 系统目标

自助台球厅系统旨在提供一个完整的解决方案，帮助台球厅实现无人化或少人化运营，用户可通过微信小程序完成查找门店、扫码开台、自助计时计费等操作，商家可通过后台系统进行门店管理、桌台管理、订单查看等操作。

### 1.2 系统架构

#### 1.2.1 整体架构
自助台球厅系统采用前后端分离的架构设计，通过RESTful API实现前后端通信。系统架构分为三层：

1. **接入层**：负责处理用户请求，包括小程序前端、商家管理后台、负载均衡等
2. **应用层**：核心业务逻辑实现，包括用户服务、门店服务、桌台服务、订单服务等
3. **数据层**：数据存储和管理，包括关系型数据库、缓存系统、对象存储等

#### 1.2.2 初期架构（单机部署）
初创阶段采用单机部署模式，适合小规模用户量和低并发场景：

```
                    +----------------+
                    |  负载均衡(可选) |
                    +--------+-------+
                             |
+----------------+  +--------v-------+  +----------------+
| 微信小程序前端  +--+   应用服务器    +--+  管理后台前端   |
+----------------+  | (All-in-One)  |  +----------------+
                    +--------+-------+
                             |
                    +--------v-------+
                    |    MySQL数据库  |
                    +----------------+
```

- 单台服务器部署全部后端服务
- 单一数据库实例
- 简单的文件系统存储静态资源
- 基础的监控和告警机制

### 1.3 技术选型

#### 1.3.1 前端技术

- **微信小程序**
  - 开发框架：原生微信小程序 / uni-app / Taro
  - UI组件库：WeUI / Vant Weapp
  - 状态管理：Mobx / Redux
  - 图表库：ECharts Mini
  - 地图组件：微信位置 SDK

- **管理后台**
  - 前端框架：Vue3 
  - UI组件库：Element Plus 
  - 状态管理：Vuex
  - 路由：Vue Router
  - HTTP客户端：Axios
  - 图表库：ECharts / AntV
  - 表格组件：vxe-table

#### 1.3.2 后端技术

- **开发语言和框架**
  - 核心语言：Java
  - 应用框架：
    - Java: Spring Boot
- **数据存储**
  - 关系型数据库：MySQL
    - ORM框架：MyBatis Plus
  - 缓存系统：Redis
  - 对象存储：MinIO
- **通信与集成**
  - API规范：RESTful
  - API文档：Swagger / OpenAPI
  - 消息队列：Kafka / RabbitMQ
  - 定时任务：Quartz / cron jobs

#### 1.3.3 DevOps工具链

- **构建与部署**
  - 容器化：Docker
  - 容器编排：Kubernetes
  - CI/CD：Jenkins / GitLab CI / GitHub Actions
  - 配置管理：Spring Cloud Config / Nacos

- **监控与运维**
  - APM：Prometheus + Grafana
  - 日志聚合：ELK Stack (Elasticsearch, Logstash, Kibana)
  - 链路追踪：SkyWalking / Jaeger
  - 告警：AlertManager / 企业微信 / 钉钉

#### 1.3.4 安全与认证

- **安全框架**：JWT，框架采用sa-token
- **加密算法**：HTTPS / SHA-256 / AES
- **数据脱敏**：字段级加密与脱敏
- **防攻击措施**：防SQL注入、XSS防护、CSRF防护
- **权限控制**：RBAC (基于角色的访问控制)

#### 1.3.5 部署环境

- **初期部署**
  - 云服务器：阿里云ECS / 腾讯云CVM
  - 数据库：云服务器部署mysql
  - 对象存储：Minio
  - CDN加速：阿里云CDN / 腾讯云CDN

- **中长期部署**（暂不需要）
  - 容器服务：Kubernetes (ACK/TKE)
  - 服务网格：Istio
  - 函数计算：阿里云函数计算 / 腾讯云SCF
  - 私有云混合部署

#### 1.3.6 演进策略

系统技术选型和架构将根据业务需求与用户规模逐步演进：

- **初创阶段**：单体架构，快速迭代，产品验证
  - 简化技术栈，快速上线
  - 服务器与数据库单机部署
  - 基础缓存与队列配置

- **成长阶段**：垂直拆分，服务独立，分库分表
  - 重构核心服务，业务垂直拆分
  - 引入Redis集群和消息队列
  - 实施数据分库分表策略
  - 建立初步的容灾方案

- **成熟阶段**：微服务架构，高并发优化，多区域部署
  - 服务完全微服务化
  - 实现容器化编排与自动扩缩容
  - 多区域部署和就近接入
  - 完善的监控、告警与自愈系统

#### 1.3.7 静态资源存储策略

静态资源（图片、视频等）的存储是系统设计中重要的一环，为了兼顾前期开发的简便性和后期扩展的灵活性，我们采用渐进式的资源存储策略，通过配置切换机制实现无缝升级。

##### 1.3.7.1 存储方案演进

**阶段一：本地文件系统存储（初期）**

适用条件：
- 门店数量少（50家以内）
- 用户量小（日活1000以内）
- 静态资源总量小于5GB
- 单机或小规模集群部署

实现方式：
- 使用服务器本地文件系统直接存储图片
- 设计合理的目录结构，例如按资源类型和业务ID组织：
  ```
  /data/images/
    ├── stores/            # 门店相关图片
    │   ├── store_1/       # 按门店ID组织
    │   │   ├── cover.jpg  # 门店封面图
    │   │   └── gallery/   # 门店环境图集
    ├── tables/            # 桌台相关图片
    │   ├── table_1.jpg
    │   └── ...
    └── qrcodes/           # 二维码图片
        ├── table_1.png
        └── ...
  ```
- 使用Spring MVC的静态资源映射提供图片访问服务

优势：
- 实现简单，开发调试方便
- 无需额外的组件和成本
- 小规模业务场景下性能足够

**阶段二：对象存储服务（中期）**

适用条件：
- 门店数量增长（100+家）
- 日活增长到5000+
- 静态资源总量超过10GB
- 需要更可靠的备份和异地容灾能力

实现方式：
- 选择方案一：云服务商提供的对象存储（阿里云OSS、腾讯云COS等）
- 选择方案二：自建MinIO对象存储服务

配置参数：
- 存储桶（Bucket）配置
- 访问密钥配置
- 基础URL配置
- 权限策略配置

优势：
- 提供更高的可用性和数据安全性
- 支持对象生命周期管理
- 支持访问控制和权限管理
- 可配置防盗链和私有资源访问控制

**阶段三：CDN加速分发（长期）**

适用条件：
- 业务扩展至多城市/地区
- 日活用户超过1万
- 对资源加载速度有较高要求
- 系统整体进入分布式架构阶段

实现方式：
- 对象存储 + CDN服务结合使用
- 对象存储作为CDN源站
- CDN服务提供全国/全球加速节点

配置参数：
- CDN域名配置
- 缓存策略配置
- HTTPS配置
- 回源规则配置

优势：
- 显著提升资源加载速度
- 减轻源站压力
- 支持HTTPS安全访问
- 提供DDoS防护能力

##### 1.3.7.2 配置切换机制设计

为了实现不同存储方案的平滑切换，我们采用策略模式设计资源存储服务：

**1. 资源服务接口定义**

```java
/**
 * 资源服务接口
 * 提供统一的资源上传、获取URL等操作
 */
public interface ResourceService {
    /**
     * 上传资源
     * @param inputStream 资源输入流
     * @param fileName 文件名
     * @param resourceType 资源类型（枚举）
     * @return 资源ID
     */
    String uploadResource(InputStream inputStream, String fileName, ResourceType resourceType);
    
    /**
     * 获取资源访问URL
     * @param resourceId 资源ID
     * @param resourceType 资源类型
     * @return 完整的访问URL
     */
    String getResourceUrl(String resourceId, ResourceType resourceType);
    
    /**
     * 删除资源
     * @param resourceId 资源ID
     * @param resourceType 资源类型
     * @return 是否删除成功
     */
    boolean deleteResource(String resourceId, ResourceType resourceType);
    
    /**
     * 生成私有资源的临时访问URL
     * @param resourceId 资源ID
     * @param resourceType 资源类型
     * @param expireTime 过期时间（分钟）
     * @return 带签名的临时访问URL
     */
    String generatePresignedUrl(String resourceId, ResourceType resourceType, int expireTime);
}
```

**2. 配置定义**

在`application.properties`或`application.yml`中定义：

```yaml
# 资源存储配置
resource:
  # 存储类型：local(本地文件系统), minio(MinIO对象存储), oss(云服务商对象存储), cdn(CDN加速)
  storage-type: local
  
  # 本地存储配置
  local:
    base-path: /data/images
    base-url: http://api.example.com/images
  
  # MinIO配置
  minio:
    endpoint: http://minio.example.com:9000
    access-key: minioadmin
    secret-key: minioadmin
    bucket: billiards-app
    base-url: http://minio.example.com:9000/billiards-app
  
  # 阿里云OSS配置
  oss:
    endpoint: https://oss-cn-hangzhou.aliyuncs.com
    access-key-id: YOUR_ACCESS_KEY
    access-key-secret: YOUR_SECRET_KEY
    bucket: billiards-app
    base-url: https://billiards-app.oss-cn-hangzhou.aliyuncs.com
  
  # CDN配置
  cdn:
    # CDN仍然需要一个存储后端，这里使用oss
    storage-backend: oss
    domain: https://static.example.com
    private-key: YOUR_CDN_PRIVATE_KEY
```

**3. 不同存储实现类**

```java
// 本地文件存储实现
@Service("localResourceService")
@ConditionalOnProperty(name = "resource.storage-type", havingValue = "local")
public class LocalResourceServiceImpl implements ResourceService {
    @Value("${resource.local.base-path}")
    private String basePath;
    
    @Value("${resource.local.base-url}")
    private String baseUrl;
    
    @Override
    public String uploadResource(InputStream inputStream, String fileName, ResourceType resourceType) {
        // 实现本地文件上传逻辑
        // ...
    }
    
    @Override
    public String getResourceUrl(String resourceId, ResourceType resourceType) {
        // 返回HTTP访问路径
        return baseUrl + "/" + getResourcePath(resourceId, resourceType);
    }
    
    // 其他方法实现...
}

// MinIO实现
@Service("minioResourceService")
@ConditionalOnProperty(name = "resource.storage-type", havingValue = "minio")
public class MinioResourceServiceImpl implements ResourceService {
    @Value("${resource.minio.endpoint}")
    private String endpoint;
    
    @Value("${resource.minio.access-key}")
    private String accessKey;
    
    // 其他配置注入...
    
    private MinioClient minioClient;
    
    @PostConstruct
    public void init() {
        // 初始化MinIO客户端
        this.minioClient = MinioClient.builder()
                .endpoint(endpoint)
                .credentials(accessKey, secretKey)
                .build();
    }
    
    @Override
    public String uploadResource(InputStream inputStream, String fileName, ResourceType resourceType) {
        // 实现MinIO上传逻辑
        // ...
    }
    
    // 其他方法实现...
}

// 阿里云OSS实现
@Service("ossResourceService")
@ConditionalOnProperty(name = "resource.storage-type", havingValue = "oss")
public class OssResourceServiceImpl implements ResourceService {
    // OSS客户端配置和实现
    // ...
}

// CDN加速实现
@Service("cdnResourceService")
@ConditionalOnProperty(name = "resource.storage-type", havingValue = "cdn")
public class CdnResourceServiceImpl implements ResourceService {
    // CDN + 存储后端实现
    // ...
}
```

**4. 服务工厂**

```java
@Service
public class ResourceServiceFactory {
    @Autowired(required = false)
    @Qualifier("localResourceService")
    private ResourceService localResourceService;
    
    @Autowired(required = false)
    @Qualifier("minioResourceService")
    private ResourceService minioResourceService;
    
    @Autowired(required = false)
    @Qualifier("ossResourceService")
    private ResourceService ossResourceService;
    
    @Autowired(required = false)
    @Qualifier("cdnResourceService")
    private ResourceService cdnResourceService;
    
    @Value("${resource.storage-type}")
    private String storageType;
    
    public ResourceService getResourceService() {
        switch (storageType) {
            case "local":
                return localResourceService;
            case "minio":
                return minioResourceService;
            case "oss":
                return ossResourceService;
            case "cdn":
                return cdnResourceService;
            default:
                throw new IllegalArgumentException("Unsupported storage type: " + storageType);
        }
    }
}
```

**5. 资源类型枚举**

```java
public enum ResourceType {
    STORE_COVER("stores/covers"),      // 门店封面图
    STORE_GALLERY("stores/gallery"),   // 门店环境图
    TABLE_IMAGE("tables"),             // 桌台图片
    QRCODE("qrcodes"),                 // 二维码图片
    USER_AVATAR("users/avatars");      // 用户头像
    
    private final String path;
    
    ResourceType(String path) {
        this.path = path;
    }
    
    public String getPath() {
        return path;
    }
}
```

##### 1.3.7.3 使用示例

**业务服务中使用资源服务**

```java
@Service
public class StoreServiceImpl implements StoreService {
    @Autowired
    private ResourceServiceFactory resourceServiceFactory;
    
    @Autowired
    private StoreRepository storeRepository;
    
    @Override
    public void updateStoreCover(String storeId, MultipartFile coverImage) throws IOException {
        // 获取当前配置的资源服务
        ResourceService resourceService = resourceServiceFactory.getResourceService();
        
        // 上传图片
        String resourceId = resourceService.uploadResource(
                coverImage.getInputStream(), 
                coverImage.getOriginalFilename(),
                ResourceType.STORE_COVER
        );
        
        // 更新门店封面图URL
        Store store = storeRepository.findById(storeId)
                .orElseThrow(() -> new EntityNotFoundException("门店不存在"));
        
        // 保存资源ID而非完整URL，URL在访问时动态生成
        store.setCoverImageId(resourceId);
        storeRepository.save(store);
    }
    
    @Override
    public StoreDTO getStoreDetail(String storeId) {
        Store store = storeRepository.findById(storeId)
                .orElseThrow(() -> new EntityNotFoundException("门店不存在"));
        
        // 获取当前配置的资源服务
        ResourceService resourceService = resourceServiceFactory.getResourceService();
        
        // 构建DTO，动态生成资源URL
        StoreDTO dto = new StoreDTO();
        // 设置基本属性...
        
        // 动态生成封面图URL
        if (store.getCoverImageId() != null) {
            dto.setCoverImageUrl(
                    resourceService.getResourceUrl(store.getCoverImageId(), ResourceType.STORE_COVER)
            );
        }
        
        // 其他处理...
        return dto;
    }
}
```

##### 1.3.7.4 数据迁移

在存储方案升级过程中，需要考虑历史数据的迁移：

**1. 数据迁移工具**

创建单独的数据迁移工具，支持：
- 从本地文件系统迁移到MinIO/OSS
- 批量处理，支持断点续传
- 迁移过程记录与校验
- 迁移后的回滚机制

**2. 迁移步骤**

1. 准备新存储环境（MinIO/OSS）
2. 运行迁移工具，批量复制资源
3. 更新配置，切换存储类型
4. 验证新系统访问
5. 保留原存储一段时间作为备份

**3. 平滑过渡策略**

在迁移期间，可采用双写策略确保数据一致性：
- 新上传的资源同时写入旧存储和新存储
- 读取时优先尝试从新存储读取，失败则回退到旧存储
- 通过监控确认所有数据访问稳定后，完全切换到新存储

通过以上设计，系统可以根据业务发展需要，灵活地在不同存储方案间切换，同时保持代码的一致性和可维护性。

### 1.4 系统角色

- 普通用户：使用小程序进行查找门店、扫码开台等操作
- 商家管理员：管理门店、桌台、查看数据统计等
- 系统管理员：管理整个系统，包括多个商家账户

## 4. 计价模块（后端逻辑）

计价模块是商家设置和管理不同球桌收费方式的核心功能，支持灵活配置多种计费策略，实现自动化的费用计算。计价模块提供标准计费和阶梯计费两种基本模式，并可针对每张桌台设置独立的计费规则。

### 4.1 标准计费模式

#### 4.1.1 功能描述
标准计费是最基础的计费方式，使用固定的单价进行计费，适用于大多数简单计费场景。

#### 4.1.2 配置参数
- **基本参数**：
  - 计费单位：分钟或小时（系统内部统一以分钟为单位计算）
  - 单价：如 0.5 元/分钟 或 30 元/小时
  - 计费精度：精确到分钟或秒（默认分钟）
- **扩展参数**：
  - 最低消费时长：设置使用的最低计费时长（如至少30分钟）
  - 封顶价格：设置单次使用的最高费用上限（如最多80元）
  - 会员价格：设置会员用户享受的优惠单价（如会员0.4元/分钟）

#### 4.1.3 计费公式
```
// 基本计费公式
费用 = 使用时长(分钟) × 单价(元/分钟)

// 考虑最低消费
if (使用时长 < 最低消费时长) {
    使用时长 = 最低消费时长
}

// 考虑封顶价格
if (费用 > 封顶价格) {
    费用 = 封顶价格
}

// 会员价格计算
if (是会员) {
    费用 = 使用时长(分钟) × 会员单价(元/分钟)
    // 同样考虑最低消费和封顶价格
}
```

#### 4.1.4 使用示例
```
配置示例：
- 单价：0.5元/分钟
- 最低消费：30分钟
- 封顶价格：80元
- 会员单价：0.4元/分钟

计费示例1：普通用户使用25分钟
- 由于低于最低消费时长，按30分钟计算
- 费用 = 30分钟 × 0.5元/分钟 = 15元

计费示例2：会员用户使用180分钟
- 费用 = 180分钟 × 0.4元/分钟 = 72元
- 未超过封顶价格，所以费用为72元

计费示例3：普通用户使用200分钟
- 费用 = 200分钟 × 0.5元/分钟 = 100元
- 超过封顶价格，所以最终费用为80元
```

### 4.2 阶梯计费模式

#### 4.2.1 功能描述
阶梯计费是一种高级计费方式，可根据使用时长分段设置不同单价，通常用于实现"使用越久越便宜"的营销策略，鼓励用户长时间使用。

#### 4.2.2 配置参数
- **阶梯设置**：
  - 支持配置多个时间区间及对应单价
  - 每个区间需设置起始时间和结束时间（最后一段可设为无限）
  - 每个区间设置独立的单价
- **扩展参数**：
  - 最低消费设置（可选）
  - 会员阶梯价格（可设置独立的会员阶梯）
  - 封顶价格（可选）

#### 4.2.3 计费公式
```
// 阶梯计费核心逻辑
总费用 = 0
对于每个时间区间 i:
    if (使用时长 > 区间i起始时间) {
        区间内时长 = min(使用时长, 区间i结束时间) - 区间i起始时间
        区间费用 = 区间内时长 × 区间i单价
        总费用 += 区间费用
    }

// 考虑最低消费和封顶价格
if (总费用 < 最低消费) {
    总费用 = 最低消费
}
if (总费用 > 封顶价格 && 封顶价格 > 0) {
    总费用 = 封顶价格
}
```

#### 4.2.4 使用示例
```
配置示例：
- 阶梯1：0-60分钟，0.5元/分钟
- 阶梯2：61-120分钟，0.4元/分钟
- 阶梯3：121分钟以上，0.3元/分钟

计费示例：用户使用150分钟
- 阶梯1：60分钟 × 0.5元/分钟 = 30元
- 阶梯2：60分钟 × 0.4元/分钟 = 24元
- 阶梯3：30分钟 × 0.3元/分钟 = 9元
- 总费用 = 30 + 24 + 9 = 63元
```

#### 4.2.5 阶梯设置界面设计
- 动态添加/删除阶梯区间
- 阶梯区间之间自动首尾衔接（前一阶段结束时间即为下一阶段开始时间）
- 支持拖拽调整阶梯范围
- 可视化展示阶梯价格曲线图

### 4.3 最低消费设置

#### 4.3.1 功能描述
最低消费设置允许商家为每张球桌设置最低消费金额或时长，确保即使用户使用时间很短，也能达到最低营收要求。

#### 4.3.2 设置选项
- **按时长设置**：
  - 设置最低使用时长（如最少30分钟）
  - 低于此时长按最低时长计费
- **按金额设置**：
  - 设置最低消费金额（如最低20元）
  - 若实际计费金额低于设定值，则按最低金额收费

#### 4.3.3 应用逻辑
- 最低消费设置可独立应用于标准计费和阶梯计费模式
- 用户开台前在页面上明确展示最低消费要求
- 系统自动在计费过程中应用最低消费规则

#### 4.3.4 特殊情况处理
- 若用户使用时长少于最低消费时长，系统自动按最低消费计算
- 开台确认页面显示最低消费提示（如"本桌最低消费30分钟"）
- 费用预览时优先考虑最低消费设置

### 4.4 会员价设置

#### 4.4.1 功能描述
会员价设置允许商家为注册会员提供优惠价格，鼓励用户注册成为会员并提高用户黏性。

#### 4.4.2 设置选项
- **会员折扣方式**：
  - 直接设置会员单价（如会员价0.4元/分钟）
  - 设置折扣比例（如会员享受8折优惠）
- **会员等级差异化**：
  - 支持设置不同会员等级的不同价格（预留扩展）
  - 如普通会员9折，金牌会员8折，钻石会员7折

#### 4.4.3 会员识别与应用
- 系统通过用户登录状态识别会员身份
- 开台确认页面同时展示普通价格和会员价格，突出优惠差异
- 计费过程自动应用会员价格计算
- 结算页面显示因会员身份节省的金额

#### 4.4.4 技术实现要点
- 会员信息与用户系统集成
- 会员价格缓存机制提高性能
- 会员价格变更后的平滑过渡策略

### 4.5 计费预览功能

#### 4.5.1 功能描述
计费预览功能允许商家在设置计费规则后直观地查看不同使用时长下的费用计算结果，验证计费设置是否符合预期。

#### 4.5.2 预览界面设计
- **时长输入区**：
  - 提供时长输入框，支持小时:分钟格式
  - 提供常用时长快捷选择（如1小时、2小时、3小时）
- **费用计算结果区**：
  - 实时显示输入时长对应的费用
  - 会员与非会员价格对比显示
  - 计费明细展开/收起

#### 4.5.3 预览计算逻辑
- 使用与实际计费相同的计算逻辑
- 实时响应时长变化，动态更新费用
- 支持阶梯计费的分段计算展示

#### 4.5.4 使用场景
- 商家新建计费规则后进行验证
- 调价前模拟不同价格方案的效果
- 会员价格设置后的对比验证

### 4.6 计费模板管理

#### 4.6.1 功能描述
计费模板管理功能允许商家创建和管理多个计费规则模板，方便快速应用于多个桌台，避免重复配置。

#### 4.6.2 模板功能设计
- **模板创建**：
  - 设置模板名称（如"工作日标准价"、"周末高峰价"）
  - 配置计费方式（标准或阶梯）
  - 设置计费参数（单价、阶梯、最低消费等）
  - 保存为模板
- **模板应用**：
  - 选择已有模板应用到单个桌台
  - 批量应用模板到多个桌台
  - 应用时可选择是否覆盖现有设置

#### 4.6.3 模板列表管理
- 展示所有可用模板列表
- 每个模板显示基本信息（名称、类型、创建时间）
- 支持模板的预览、编辑、删除操作
- 显示每个模板当前应用的桌台数量

#### 4.6.4 模板版本控制
- 记录模板修改历史
- 模板更新后可选择是否同步更新已应用该模板的桌台
- 支持模板回滚到历史版本

### 4.7 计费过程实时展示

#### 4.7.1 功能描述
计费过程实时展示功能在用户使用球桌期间，清晰地展示当前累计时长、实时费用和计费规则，提升使用透明度和用户体验。

#### 4.7.2 用户端展示设计
- **计时区域**：
  - 大号显示当前累计时长（时:分:秒）
  - 显示开始时间和预估结束时间
- **费用区域**：
  - 实时更新当前累计费用
  - 费用变化时有轻微动效提示
  - 显示当前所处计费阶段（阶梯计费时）

#### 4.7.3 计费规则展示
- 简洁展示当前适用的计费规则
- 阶梯计费时显示当前所处阶段和下一阶段信息
- 提供展开/收起详细计费规则的选项

#### 4.7.4 技术实现方案
- 前端计时器定时更新显示（每秒更新）
- 前端根据计费规则计算实时费用
- 定期（如每分钟）与服务端同步，确保计费准确性
- 使用动画和过渡效果优化用户体验

#### 4.7.5 显示优化
- 深色模式设计，减少屏幕亮度对球员的干扰
- 大字号设计，便于远距离查看
- 简洁明了的界面，减少干扰元素

### 4.8 使用详情页

#### 4.8.1 页面定位与入口
- **主要入口**：用户点击首页"进行中卡片"进入
- **辅助入口**：用户可从订单列表中点击进行中订单进入
- **页面位置**：独立页面，不占用主导航位置

#### 4.8.2 页面架构
- **顶部区域**：
  - 门店标识（Logo和名称）
  - 桌号信息
  - 当前状态指示
- **计费信息区**：
  - 开始时间
  - 已使用时长（实时更新）
  - 当前计费金额（实时更新）
  - 计费规则说明
- **计费详情区**：
  - 计费方式展示（标准或阶梯）
  - 时段费率明细
  - 当前适用费率高亮显示
  - 预估结束费用计算器
- **操作区域**：
  - 结束使用按钮（主要按钮）
  - 续时选项（如允许）
  - 联系门店按钮
  - 分享按钮（可选功能）

#### 4.8.3 显示内容详情
- **时间显示**：
  - 开始时间：精确到分，如"今天 14:30 开始"
  - 当前时长：小时和分钟，如"已使用 2小时25分钟"
  - 预计结束：如选择续时，显示预计结束时间点
- **费用显示**：
  - 当前费用：大字体显示，如"¥36.50"
  - 费率说明：当前适用费率，如"当前费率：¥15/小时"
  - 阶梯提示：如"再使用30分钟进入优惠费率¥12/小时"
- **订单信息**：
  - 订单编号（用于客服沟通）
  - 下单时间
  - 支付方式（如有预授权或预付）

#### 4.8.4 实时更新机制
- **计时逻辑**：
  - 前端使用精确计时器，每秒更新时间显示
  - 每隔1分钟与服务器同步实际计时
  - 同步失败时显示"本地计时"标记
- **费用计算**：
  - 前端根据缓存的计费规则实时计算展示
  - 每隔5分钟与服务器同步实际费用
  - 费用更新时有数字变化动效

#### 4.8.5 交互逻辑
- **续时操作**：
  - 点击"续时"按钮展开续时选项面板
  - 提供常用续时时长（+30分钟、+1小时、+2小时等）
  - 确认续时后更新预计结束时间和费用
- **结束使用**：
  - 点击"结束使用"弹出确认对话框
  - 显示最终费用预估和注意事项
  - 确认后跳转到支付确认页面
  - 支付完成后显示本次使用总结页
- **返回首页**：
  - 提供明确的返回首页按钮
  - 保留顶部导航"返回"按钮

#### 4.8.6 异常状态处理
- **网络异常**：
  - 显示"网络连接异常"提示条
  - 本地继续计时，但标记为"待同步"状态
  - 网络恢复后自动同步服务器数据
- **计费异常**：
  - 费用计算异常时显示"费用计算中"
  - 提供手动刷新按钮
  - 长时间异常提供联系客服入口
- **商家干预**：
  - 商家后台修改计费规则时推送通知
  - 商家强制结束时弹出通知，引导支付流程

## 5. 后台管理模块

后台管理模块是商家运营和管理台球厅的核心系统，提供全面的门店管理、桌台管理、用户管理、计价管理和数据分析功能，帮助商家实现智能化、高效化的运营。

### 5.1 商家账户管理（暂不实现）

#### 5.1.1 账户体系设计
- **账户类型**：
  - 超级管理员：拥有系统所有权限
  - 门店管理员：管理特定门店的所有功能
  - 普通员工：执行日常操作任务
- **账户信息**：
  - 基本信息：账户名、手机号、邮箱、姓名
  - 安全信息：密码、安全问题、验证方式
  - 授权信息：所属角色、权限范围、可管理门店

#### 5.1.2 账户管理功能
- **账户创建**：
  - 手动创建子账户
  - 设置初始密码和权限范围
  - 发送邀请链接激活账户
- **账户编辑**：
  - 修改账户基本信息
  - 调整权限设置
  - 重置密码
- **账户停用/启用**：
  - 临时冻结账户
  - 永久删除账户
  - 账户解冻

#### 5.1.3 权限管理系统
- **角色管理**：
  - 预设角色（超管、店长、前台等）
  - 自定义角色创建
  - 角色权限编辑
- **权限分配**：
  - 功能模块权限（查看/编辑/删除）
  - 数据范围权限（全部门店/特定门店）
  - 操作级权限（如强制结束他人开台）
- **权限审计**：
  - 账户操作日志记录
  - 敏感操作提醒
  - 操作行为分析

#### 5.1.4 登录安全设计
- **多因素认证**：
  - 支持短信验证码登录
  - 可选 Google 身份验证器
  - 记住登录状态功能
- **异常登录保护**：
  - 检测非常用设备登录
  - IP 异常变化提醒
  - 登录失败次数限制
- **安全策略设置**：
  - 密码复杂度要求
  - 密码过期策略
  - 登录会话有效期

### 5.2 多门店管理

#### 5.2.1 门店信息管理
- **基础信息**：
  - 门店名称：支持中英文名称设置
  - 门店介绍：富文本编辑器支持格式化描述
  - 营业时间：支持设置每日营业时段，可区分工作日/周末
  - 联系方式：电话、微信、邮箱等
- **地理位置**：
  - 详细地址信息（省市区街道门牌号）
  - 地图定位（经纬度坐标）
  - 导航指引信息
- **门店照片**：
  - 门店封面图（显示在首页列表）
  - 环境照片集（最多12张）
  - 全景照片（可选）

#### 5.2.2 门店运营管理
- **营业状态控制**：
  - 正常营业：按设定时间自动切换状态
  - 临时休息：手动设置特定时间段停业
  - 长期歇业：如装修、转让等情况
- **公告管理**：
  - 发布店铺公告内容
  - 设置公告有效期
  - 紧急公告推送功能
- **特殊活动**：
  - 优惠活动设置（如节假日折扣）
  - 活动时间段设置
  - 活动规则配置

#### 5.2.3 门店列表与筛选
- **列表视图**：
  - 表格式展示所有门店基本信息
  - 卡片式展示门店详情和状态
  - 支持列表/卡片视图切换
- **筛选功能**：
  - 按营业状态筛选
  - 按区域位置筛选
  - 按营业额/客流量排序
- **门店对比**：
  - 选择多家门店进行数据对比
  - 生成对比报表
  - 查看运营差异分析

#### 5.2.4 门店配置管理
- **基础配置**：
  - 货币单位设置
  - 税率设置
  - 语言设置
- **显示设置**：
  - 门店在小程序中的展示顺序
  - 是否显示距离信息
  - 是否显示空闲桌数
- **开放设置**：
  - 设置门店服务范围（如5公里内可见）
  - 设置最大可见客户数
  - 新店试运营模式

### 5.3 桌台管理

#### 5.3.1 桌台信息管理
- **基础信息设置**：
  - 桌号：支持数字和自定义编号（如A1, B2）
  - 桌台类型：专业级、大师级等分类
  - 桌台品牌和型号
  - 球杆数量和类型
- **状态信息**：
  - 实时状态：空闲/使用中/维修中/已预约
  - 使用频率统计：平均每日使用次数和时长
  - 上次维护时间
- **位置信息**：
  - 所属区域（如大厅、VIP区）
  - 位置描述（如靠窗、角落）
  - 店内位置图上标记

#### 5.3.2 桌台管理功能
- **批量操作**：
  - 批量添加桌台
  - 批量设置状态（如全部停用）
  - 批量更新计费规则
- **桌台排序**：
  - 自定义桌台显示顺序
  - 按区域自动排序
  - 按状态排序
- **桌台启用/停用**：
  - 单个桌台启用/停用设置
  - 停用原因记录
  - 计划性停用（预设时间自动恢复）

#### 5.3.3 桌台监控与控制
- **实时状态监控**：
  - 可视化展示所有桌台当前状态
  - 使用中桌台显示使用时长和预估费用
  - 状态变化实时推送通知
- **远程控制**：
  - 远程强制结束使用（特殊情况）
  - 远程锁定/解锁桌台
  - 远程调整计费规则（实时生效）
- **异常处理**：
  - 桌台故障标记
  - 维修管理流程
  - 安排检修计划

#### 5.3.4 桌台二维码管理
- **二维码生成**：
  - 自动为每张桌台生成唯一二维码
  - 支持重新生成二维码（如损坏或丢失）
  - 二维码有效期管理
- **打印与应用**：
  - 批量导出二维码图片
  - 提供标准尺寸打印模板
  - 桌台贴纸设计与打印
- **二维码数据追踪**：
  - 记录每个二维码的扫描次数
  - 扫码开台转化率统计
  - 异常扫码记录分析

### 5.4 计价管理

计价管理详见[计价模块](#4-计价模块)，在后台管理中主要关注以下管理功能：

#### 5.4.1 计费规则管理界面
- **规则列表**：
  - 展示所有已创建的计费规则
  - 显示每条规则适用的桌台数量
  - 规则状态指示（启用/停用）
- **规则创建/编辑**：
  - 规则基本信息设置
  - 可视化编辑界面
  - 预览计算效果
- **规则应用**：
  - 将规则应用到指定桌台
  - 批量应用功能
  - 规则变更历史记录

#### 5.4.2 特殊计费策略
- **时段差异化计费**：
  - 工作日/周末不同价格
  - 早晚场差异化定价
  - 节假日特殊价格
- **会员特惠政策**：
  - 会员折扣设置
  - 会员积分兑换时长
  - 限时优惠活动
- **套餐计费模式**：
  - 包时段套餐（如4小时套餐）
  - 包天套餐
  - 多人拼桌优惠

#### 5.4.3 计费报表与分析
- **计费收入统计**：
  - 按时间段统计收入
  - 按桌台类型分析收入结构
  - 计费方式对比分析
- **优化建议**：
  - 基于历史数据的价格优化建议
  - 热门时段分析
  - 计价策略效果评估
- **异常分析**：
  - 异常计费记录筛查
  - 用户投诉计费案例分析
  - 系统计费错误检测

### 5.5 订单管理

#### 5.5.1 订单列表与查询
- **订单列表界面**：
  - 所有开台记录的列表展示
  - 关键信息显示：订单号、门店、桌号、用户、时长、金额
  - 状态标记：进行中/已完成/异常
- **高级筛选**：
  - 时间范围：今日/本周/本月/自定义日期
  - 门店筛选：选择特定门店
  - 状态筛选：按订单状态筛选
  - 金额区间：设置金额范围
- **搜索功能**：
  - 按订单号精确搜索
  - 按用户手机号/昵称搜索
  - 按桌台号搜索

#### 5.5.2 订单详情查看
- **基础信息**：
  - 订单完整信息展示
  - 用户信息关联显示
  - 时间轴展示订单全流程
- **计费明细**：
  - 详细计费规则展示
  - 计费过程分解说明
  - 优惠信息展示
- **操作记录**：
  - 订单相关所有操作日志
  - 异常情况记录
  - 管理员处理痕迹

#### 5.5.3 订单处理功能
- **手动干预**：
  - 强制结束订单
  - 修改订单金额（需权限）
  - 取消订单（特殊情况）
- **异常处理**：
  - 订单投诉处理流程
  - 退款申请处理
  - 异常结单修复
- **批量操作**：
  - 批量导出订单数据
  - 批量打印小票
  - 批量归档历史订单

#### 5.5.4 实时订单监控
- **进行中订单看板**：
  - 实时展示所有进行中订单
  - 按门店分组显示
  - 超长时间订单预警
- **即时通知**：
  - 新订单开始提醒
  - 异常订单预警
  - 高额订单完成提醒
- **数据统计**：
  - 今日订单总览
  - 实时营收统计
  - 高峰期预测

### 5.6 用户管理

#### 5.6.1 用户基础信息
- **用户列表**：
  - 所有注册用户的列表
  - 显示关键信息：头像、昵称、手机号、注册时间
  - 会员状态标识
- **用户详情**：
  - 基本资料：姓名、性别、年龄等
  - 联系方式：手机、微信、邮箱
  - 注册信息：注册时间、来源渠道
  - 会员信息：会员等级、积分、到期时间

#### 5.6.2 用户数据分析（暂不需要）
- **使用习惯分析**：
  - 常用门店统计
  - 平均使用时长
  - 偏好使用时段
- **消费分析**：
  - 历史消费金额统计
  - 平均单次消费
  - 消费频率分析
- **行为轨迹**：
  - 近期访问记录
  - 搜索记录分析
  - 互动行为记录

#### 5.6.3 会员管理
- **会员等级设置**：
  - 等级体系定义（普通/银牌/金牌/钻石等）
  - 升级规则设置
  - 等级权益配置
- **会员优惠设置**：
  - 不同等级会员折扣设置
  - 会员专属活动配置
  - 积分规则设置
- **会员营销**：
  - 会员群体筛选
  - 定向优惠推送
  - 会员活动策划

#### 5.6.4 用户互动管理
- **消息推送**：
  - 系统公告推送
  - 个性化优惠通知
  - 活动邀请推送
- **用户反馈**：
  - 用户评价管理
  - 投诉处理流程
  - 建议收集与分析
- **客户沟通**：
  - 在线客服集成
  - 用户问题快速回复
  - 服务质量跟踪

### 5.7 数据统计与分析

#### 5.7.1 实时数据看板

##### 5.7.1.1 数据看板总览
- **设计目标**：为管理者提供业务关键指标的直观实时展示，支持快速决策和异常监控
- **主要功能**：聚合展示当日/本周/本月核心业务数据，支持多维度切换和下钻分析
- **关键指标**：
  - 营业额实时统计（总营收、预计营收、同比增长）
  - 开台数量与使用率（总开台次数、桌台利用率）
  - 实时客流量（当前使用人数、高峰预测）
  - 异常订单监控（超长时间订单、支付异常订单）

##### 5.7.1.2 门店仪表盘
- **今日营业总览**：
  - 营业状态：各门店开业/休息状态
  - 今日营收：实时更新的营业额统计
  - 桌台使用情况：空闲/占用/维修比例
  - 预计今日总营收（基于历史数据和当前使用趋势）
- **门店对比分析**：
  - 各门店营业额排行榜
  - 桌台利用率对比图表
  - 客单价对比分析
  - 会员消费占比对比
- **时段热力分析**：
  - 24小时客流量分布热力图
  - 高峰期识别与标记
  - 时段收入对比柱状图
  - 各时段桌台使用率折线图

##### 5.7.1.3 交互设计
- **实时刷新机制**：
  - 自动刷新：关键指标每5分钟自动刷新一次
  - 手动刷新：支持用户手动触发数据刷新
  - 数据时间戳：显示最近数据更新时间
- **筛选与下钻**：
  - 时间范围快速选择器：今日/本周/本月/自定义
  - 门店多选筛选器：支持选择特定门店查看数据
  - 数据下钻功能：点击图表元素查看详情数据
- **预警机制**：
  - 关键指标异常预警（营收大幅下滑、使用率异常）
  - 视觉化预警标识（颜色编码、闪烁提醒）
  - 预警阈值自定义配置

##### 5.7.1.4 数据可视化规范
- **图表类型选择**：
  - 时间序列数据：折线图、面积图
  - 分类对比数据：柱状图、条形图
  - 占比数据：饼图、环形图
  - 多维分析：散点图、气泡图
  - 趋势判断：仪表盘、进度条
- **配色方案**：
  - 品牌主色调为基础
  - 数据对比配色：有对比度但和谐的配色方案
  - 状态配色：成功（绿色）、警告（黄色）、危险（红色）、禁用（灰色）
  - 保持一致性：相同指标在不同图表中保持相同颜色
- **布局设计**：
  - 模块化结构：相关数据分组展示
  - 信息层级：重要指标突出显示
  - 响应式设计：适应不同屏幕尺寸
  - 支持自定义布局和收藏常用图表

#### 5.7.2 历史数据分析
- **趋势图表**：
  - 日/周/月/年营收趋势
  - 用户增长趋势
  - 使用频率变化趋势
- **详细报表**：
  - 营收明细报表
  - 桌台使用率报表
  - 会员消费报表
- **对比分析**：
  - 同比/环比分析
  - 不同时期数据对比
  - 不同门店表现对比

#### 5.7.3 经营分析工具
- **盈利能力分析**：
  - 门店收入结构分析
  - 单桌台产出分析
  - 时段盈利能力分析
- **运营效率分析**：
  - 桌台周转率分析
  - 人均使用时长分析
  - 峰谷平衡分析
- **智能预测**：
  - 未来营收预测
  - 客流量预测
  - 淡旺季预测

#### 5.7.4 数据导出与共享
- **导出功能**：
  - 数据Excel导出
  - PDF报表生成
  - 自定义数据筛选导出
- **定时报表**：
  - 设置定时生成报表
  - 报表自动发送至指定邮箱
  - 周期性报表模板设置
- **数据可视化分享**：
  - 生成可分享的数据图表
  - 报表链接分享
  - 数据分析结果推送

### 5.8 系统设置

#### 5.8.1 基础设置
- **商家信息设置**：
  - 企业基本信息
  - 品牌Logo和色彩设定
  - 联系方式与客服信息
- **小程序设置**：
  - 首页Banner配置
  - 导航菜单自定义
  - 活动推广位设置
- **通知设置**：
  - 系统通知接收人设置
  - 通知方式选择（短信/微信/邮件）
  - 通知类型与级别设置

#### 5.8.2 高级设置
- **安全设置**：
  - 后台访问IP限制
  - 敏感操作二次验证
  - 操作日志保存期限
- **自动化规则**：
  - 自动开关店设置
  - 系统自动备份策略
  - 数据定期清理策略
- **集成配置**：
  - 支付接口配置
  - 短信服务设置
  - 地图服务配置
  - 第三方系统对接

#### 5.8.3 开发者设置
- **API管理**：
  - API接口密钥管理
  - 接口访问权限控制
  - 接口调用统计
- **开发环境**：
  - 测试环境切换
  - 功能测试工具
  - 调试日志设置
- **版本管理**：
  - 系统版本信息
  - 更新日志查看
  - 新功能预览

### 5.9 移动管理应用（🔄 待优化）

#### 5.9.1 功能概述
- **应用定位**：为商家管理人员提供随时掌握业务状态的移动工具
- **目标用户**：门店经理、店长、运营管理人员
- **核心价值**：不受办公场所限制，实时监控门店运营，快速处理业务异常

#### 5.9.2 主要功能
- **实时监控中心**：
  - 门店状态概览
  - 桌台使用情况实时展示
  - 关键业务指标实时更新
  - 异常事件通知

- **远程桌台管理**：
  - 查看各桌台状态和使用信息
  - 远程锁定/解锁桌台
  - 远程强制结束订单
  - 查看桌台使用历史

- **订单处理中心**：
  - 查看当日/历史订单列表
  - 订单详情查看
  - 订单异常处理
  - 退款申请处理

- **数据看板**：
  - 今日营业数据概览
  - 营收趋势简易图表
  - 关键指标卡片展示
  - 门店对比简报

- **推送通知**：
  - 新订单通知
  - 异常订单提醒
  - 营业指标达成提示
  - 系统异常警报

#### 5.9.3 设计原则
- **简化操作**：相比管理后台，优化关键流程，减少操作步骤
- **重点突出**：突出展示需要及时关注和处理的信息
- **离线支持**：核心功能支持弱网或短时离线操作，恢复联网后同步
- **安全保障**：移动端访问需要额外安全验证，敏感操作二次确认

#### 5.9.4 技术方案
- **开发框架**：基于Flutter实现跨平台支持（iOS/Android）
- **认证方式**：与管理系统共享账户体系，支持指纹/FaceID快速登录
- **数据同步**：使用WebSocket实现实时数据推送
- **本地存储**：关键数据本地缓存，支持离线查看

## 6. 数据库设计

数据库设计为整个系统提供数据存储和管理支持，主要采用MySQL关系型数据库。

### 6.1 数据表设计

#### 6.1.1 用户表(user)

| 字段名            | 数据类型      | 说明                       | 约束              |
|------------------|--------------|---------------------------|------------------|
| id               | varchar(32)  | 用户唯一标识符              | 主键              |
| openid           | varchar(64)  | 微信OpenID                 | 唯一, 非空        |
| unionid          | varchar(64)  | 微信UnionID                | 可空              |
| nickname         | varchar(32)  | 用户昵称                    | 非空              |
| avatar           | varchar(255) | 用户头像URL                 | 可空              |
| gender           | tinyint(1)   | 性别 0-未知 1-男 2-女        | 默认0             |
| phone            | varchar(20)  | 手机号码                    | 可空, 索引         |
| is_member        | tinyint(1)   | 是否是会员 0-否 1-是         | 默认0             |
| member_level     | tinyint(2)   | 会员等级                    | 默认0             |
| member_expire    | datetime     | 会员过期时间                 | 可空              |
| points           | int          | 积分                       | 默认0             |
| create_time      | datetime     | 创建时间                    | 非空              |
| update_time      | datetime     | 更新时间                    | 非空              |
| last_login_time  | datetime     | 最后登录时间                 | 可空              |
| status           | tinyint(1)   | 状态 0-正常 1-禁用           | 默认0             |

#### 6.1.2 商家表(merchant)

| 字段名            | 数据类型      | 说明                       | 约束              |
|------------------|--------------|---------------------------|------------------|
| id               | varchar(32)  | 商家唯一标识符              | 主键              |
| name             | varchar(64)  | 商家名称                    | 非空              |
| logo             | varchar(255) | 商家Logo                   | 可空              |
| contact_name     | varchar(32)  | 联系人姓名                  | 非空              |
| contact_phone    | varchar(20)  | 联系人电话                  | 非空              |
| business_license | varchar(64)  | 营业执照号                  | 非空              |
| address          | varchar(255) | 公司地址                    | 非空              |
| create_time      | datetime     | 创建时间                    | 非空              |
| update_time      | datetime     | 更新时间                    | 非空              |
| status           | tinyint(1)   | 状态 0-正常 1-禁用           | 默认0             |

#### 6.1.3 员工表(staff)

| 字段名            | 数据类型      | 说明                       | 约束              |
|------------------|--------------|---------------------------|------------------|
| id               | varchar(32)  | 员工唯一标识符              | 主键              |
| merchant_id      | varchar(32)  | 所属商家ID                 | 外键, 非空         |
| username         | varchar(32)  | 登录用户名                  | 唯一, 非空        |
| password         | varchar(128) | 密码(加密存储)              | 非空              |
| real_name        | varchar(32)  | 真实姓名                    | 非空              |
| phone            | varchar(20)  | 手机号码                    | 非空              |
| role_id          | varchar(32)  | 角色ID                     | 外键, 非空         |
| create_time      | datetime     | 创建时间                    | 非空              |
| update_time      | datetime     | 更新时间                    | 非空              |
| status           | tinyint(1)   | 状态 0-正常 1-禁用           | 默认0             |

#### 6.1.4 门店表(store)

| 字段名            | 数据类型      | 说明                       | 约束              |
|------------------|--------------|---------------------------|------------------|
| id               | varchar(32)  | 门店唯一标识符              | 主键              |
| merchant_id      | varchar(32)  | 所属商家ID                 | 外键, 非空         |
| name             | varchar(64)  | 门店名称                    | 非空              |
| cover_image      | varchar(255) | 门店封面图URL               | 可空              |
| province         | varchar(32)  | 省份                       | 非空              |
| city             | varchar(32)  | 城市                       | 非空              |
| district         | varchar(32)  | 区县                       | 非空              |
| address          | varchar(255) | 详细地址                    | 非空              |
| latitude         | decimal(10,7)| 纬度坐标                    | 非空              |
| longitude        | decimal(10,7)| 经度坐标                    | 非空              |
| contact_phone    | varchar(20)  | 联系电话                    | 非空              |
| business_hours   | varchar(128) | 营业时间                    | 非空              |
| announcement     | varchar(255) | 公告                       | 可空              |
| create_time      | datetime     | 创建时间                    | 非空              |
| update_time      | datetime     | 更新时间                    | 非空              |
| status           | tinyint(1)   | 状态 0-正常 1-休息 2-停业     | 默认0             |

#### 6.1.5 桌台表(table)

| 字段名            | 数据类型      | 说明                       | 约束              |
|------------------|--------------|---------------------------|------------------|
| id               | varchar(32)  | 桌台唯一标识符              | 主键              |
| store_id         | varchar(32)  | 所属门店ID                 | 外键, 非空         |
| table_number     | varchar(16)  | 桌台编号                    | 非空              |
| table_type       | tinyint(2)   | 桌台类型 1-普通 2-专业 3-大师  | 非空              |
| description      | varchar(255) | 桌台描述                    | 可空              |
| image            | varchar(255) | 桌台图片URL                 | 可空              |
| qrcode_url       | varchar(255) | 二维码URL                  | 非空              |
| price_rule_id    | varchar(32)  | 计费规则ID                 | 外键, 非空         |
| create_time      | datetime     | 创建时间                    | 非空              |
| update_time      | datetime     | 更新时间                    | 非空              |
| status           | tinyint(1)   | 状态 0-空闲 1-使用中 2-维修中 3-锁定 | 默认0        |

#### 6.1.6 计费规则表(price_rule)

| 字段名            | 数据类型      | 说明                       | 约束              |
|------------------|--------------|---------------------------|------------------|
| id               | varchar(32)  | 规则唯一标识符              | 主键              |
| merchant_id      | varchar(32)  | 所属商家ID                 | 外键, 非空         |
| name             | varchar(64)  | 规则名称                    | 非空              |
| rule_type        | tinyint(1)   | 规则类型 1-标准计费 2-阶梯计费  | 非空              |
| price_unit       | decimal(10,2)| 单价(元/分钟)               | rule_type=1时非空 |
| member_price     | decimal(10,2)| 会员价格(元/分钟)            | 可空              |
| min_minutes      | int          | 最低消费时长(分钟)           | 默认0             |
| max_price        | decimal(10,2)| 封顶价格                    | 可空              |
| ladder_rules     | text         | 阶梯计费规则JSON            | rule_type=2时非空 |
| create_time      | datetime     | 创建时间                    | 非空              |
| update_time      | datetime     | 更新时间                    | 非空              |
| status           | tinyint(1)   | 状态 0-启用 1-停用           | 默认0             |

#### 6.1.7 订单表(order)

| 字段名            | 数据类型      | 说明                       | 约束              |
|------------------|--------------|---------------------------|------------------|
| id               | varchar(32)  | 订单唯一标识符              | 主键              |
| order_no         | varchar(32)  | 订单编号                    | 唯一, 非空        |
| user_id          | varchar(32)  | 用户ID                     | 外键, 非空         |
| store_id         | varchar(32)  | 门店ID                     | 外键, 非空         |
| table_id         | varchar(32)  | 桌台ID                     | 外键, 非空         |
| price_rule_id    | varchar(32)  | 计费规则ID                 | 外键, 非空         |
| start_time       | datetime     | 开始时间                    | 非空              |
| end_time         | datetime     | 结束时间                    | 可空              |
| duration         | int          | 使用时长(分钟)               | 默认0             |
| original_amount  | decimal(10,2)| 原始金额                    | 默认0             |
| discount_amount  | decimal(10,2)| 折扣金额                    | 默认0             |
| actual_amount    | decimal(10,2)| 实际金额                    | 默认0             |
| payment_status   | tinyint(1)   | 支付状态 0-未支付 1-已支付 2-退款 | 默认0           |
| create_time      | datetime     | 创建时间                    | 非空              |
| update_time      | datetime     | 更新时间                    | 非空              |
| status           | tinyint(1)   | 状态 0-进行中 1-已完成 2-已取消  | 默认0             |

## 7. API接口设计

### 7.1 用户接口

#### 7.1.1 用户授权登录接口

- **路径**：`/api/user/auth/login`
- **方式**：POST
- **说明**：微信授权登录
- **请求参数**：
  - code：微信临时凭证
  - userInfo：微信用户信息
- **响应**：token, userId, isNewUser, userInfo

#### 7.1.2 获取用户信息接口

- **路径**：`/api/user/info`
- **方式**：GET
- **说明**：获取当前用户信息
- **请求头**：Authorization: Bearer {token}
- **响应**：用户基本信息（ID, 昵称, 头像, 会员状态等）

### 7.2 门店接口

#### 7.2.1 获取附近门店列表

- **路径**：`/api/stores/nearby`
- **方式**：GET
- **说明**：获取用户附近门店
- **请求参数**：
  - latitude：纬度（可选）
  - longitude：经度（可选）
  - radius：搜索半径，默认5000米
  - status：状态筛选，默认0（营业中）
  - keyword：搜索关键词
  - pageNum：页码，默认1
  - pageSize：每页数量，默认10
- **响应**：门店列表分页数据

#### 7.2.2 获取门店详情

- **路径**：`/api/stores/{storeId}`
- **方式**：GET
- **说明**：获取指定门店详情
- **路径参数**：storeId：门店ID
- **响应**：门店详细信息

### 7.3 桌台接口

#### 7.3.1 扫码识别桌台

- **路径**：`/api/tables/scan`
- **方式**：POST
- **说明**：扫描桌台二维码
- **请求参数**：qrCode：二维码内容
- **响应**：桌台信息（包含状态、计费规则等）

#### 7.3.2 获取门店桌台列表

- **路径**：`/api/stores/{storeId}/tables`
- **方式**：GET
- **说明**：获取指定门店的所有桌台
- **路径参数**：storeId：门店ID
- **请求参数**：
  - status：状态筛选
  - type：桌台类型筛选
- **响应**：桌台列表数据

### 7.4 订单接口

#### 7.4.1 创建开台订单

- **路径**：`/api/orders`
- **方式**：POST
- **说明**：用户确认开台创建订单
- **请求头**：Authorization: Bearer {token}
- **请求参数**：
  - tableId：桌台ID
  - storeId：门店ID
- **响应**：订单基本信息

#### 7.4.2 结束使用订单

- **路径**：`/api/orders/{orderId}/end`
- **方式**：POST
- **说明**：用户结束使用
- **请求头**：Authorization: Bearer {token}
- **路径参数**：orderId：订单ID
- **响应**：订单结算信息

#### 7.4.3 获取用户订单列表

- **路径**：`/api/user/orders`
- **方式**：GET
- **说明**：获取用户订单历史
- **请求头**：Authorization: Bearer {token}
- **请求参数**：
  - status：订单状态筛选
  - startDate：开始日期
  - endDate：结束日期
  - pageNum：页码，默认1
  - pageSize：每页数量，默认10
- **响应**：订单列表分页数据

### 7.5 支付接口

#### 7.5.1 订单支付

- **路径**：`/api/payment/pay`
- **方式**：POST
- **说明**：创建支付订单
- **请求头**：Authorization: Bearer {token}
- **请求参数**：
  - orderId：订单ID
  - paymentMethod：支付方式
- **响应**：支付参数（如微信支付的prepay_id）

#### 7.5.2 查询支付状态

- **路径**：`/api/payment/status/{orderId}`
- **方式**：GET
- **说明**：查询订单支付状态
- **请求头**：Authorization: Bearer {token}
- **路径参数**：orderId：订单ID
- **响应**：支付状态信息

### 7.6 管理后台接口

#### 7.6.1 门店管理接口

##### 7.6.1.1 创建门店

- **路径**：`/api/admin/stores`
- **方式**：POST
- **说明**：创建新门店
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - name: 门店名称
  - merchantId: 所属商家ID
  - coverImage: 门店封面图
  - province: 省份
  - city: 城市
  - district: 区县
  - address: 详细地址
  - latitude: 纬度坐标
  - longitude: 经度坐标
  - contactPhone: 联系电话
  - businessHours: 营业时间
  - announcement: 公告(可选)
- **响应**：新创建的门店信息

##### 7.6.1.2 更新门店信息

- **路径**：`/api/admin/stores/{storeId}`
- **方式**：PUT
- **说明**：更新门店信息
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：storeId：门店ID
- **请求参数**：与创建门店参数相同，但所有字段都是可选的
- **响应**：更新后的门店信息

##### 7.6.1.3 删除门店

- **路径**：`/api/admin/stores/{storeId}`
- **方式**：DELETE
- **说明**：删除指定门店
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：storeId：门店ID
- **响应**：操作结果

##### 7.6.1.4 获取所有门店列表

- **路径**：`/api/admin/stores`
- **方式**：GET
- **说明**：获取商家所有门店列表
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - merchantId：商家ID
  - status：状态筛选
  - keyword：关键词搜索
  - pageNum：页码，默认1
  - pageSize：每页数量，默认10
- **响应**：门店列表分页数据

##### 7.6.1.5 获取门店详情

- **路径**：`/api/admin/stores/{storeId}`
- **方式**：GET
- **说明**：获取指定门店详细信息
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：storeId：门店ID
- **响应**：门店详细信息

##### 7.6.1.6 更新门店状态

- **路径**：`/api/admin/stores/{storeId}/status`
- **方式**：PATCH
- **说明**：更新门店营业状态
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：storeId：门店ID
- **请求参数**：
  - status：新状态值（0-正常营业 1-休息中 2-停业）
  - reason：状态变更原因（可选）
- **响应**：更新后的门店状态信息

#### 7.6.2 桌台管理接口

##### 7.6.2.1 创建桌台

- **路径**：`/api/admin/tables`
- **方式**：POST
- **说明**：在指定门店创建新桌台
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - storeId：所属门店ID
  - tableNumber：桌台编号
  - tableType：桌台类型（1-普通 2-专业 3-大师）
  - description：桌台描述（可选）
  - image：桌台图片URL（可选）
  - priceRuleId：计费规则ID
- **响应**：新创建的桌台信息，包含自动生成的二维码URL

##### 7.6.2.2 批量创建桌台

- **路径**：`/api/admin/tables/batch`
- **方式**：POST
- **说明**：批量创建多个桌台
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - storeId：所属门店ID
  - startNumber：起始编号
  - count：创建数量
  - tableType：桌台类型
  - priceRuleId：计费规则ID
  - prefix：编号前缀（可选）
- **响应**：批量创建的桌台列表

##### 7.6.2.3 更新桌台信息

- **路径**：`/api/admin/tables/{tableId}`
- **方式**：PUT
- **说明**：更新桌台信息
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：tableId：桌台ID
- **请求参数**：与创建桌台参数相同，但所有字段都是可选的
- **响应**：更新后的桌台信息

##### 7.6.2.4 删除桌台

- **路径**：`/api/admin/tables/{tableId}`
- **方式**：DELETE
- **说明**：删除指定桌台
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：tableId：桌台ID
- **响应**：操作结果

##### 7.6.2.5 获取门店桌台列表

- **路径**：`/api/admin/stores/{storeId}/tables`
- **方式**：GET
- **说明**：获取指定门店的所有桌台
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：storeId：门店ID
- **请求参数**：
  - status：状态筛选
  - type：桌台类型筛选
  - keyword：关键词搜索
  - pageNum：页码，默认1
  - pageSize：每页数量，默认20
- **响应**：桌台列表分页数据

##### 7.6.2.6 获取桌台详情

- **路径**：`/api/admin/tables/{tableId}`
- **方式**：GET
- **说明**：获取指定桌台详细信息
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：tableId：桌台ID
- **响应**：桌台详细信息，包含使用统计数据

##### 7.6.2.7 更新桌台状态

- **路径**：`/api/admin/tables/{tableId}/status`
- **方式**：PATCH
- **说明**：更新桌台状态
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：tableId：桌台ID
- **请求参数**：
  - status：新状态值（0-空闲 1-使用中 2-维修中 3-锁定）
  - reason：状态变更原因（可选）
- **响应**：更新后的桌台状态信息

##### 7.6.2.8 重新生成桌台二维码

- **路径**：`/api/admin/tables/{tableId}/qrcode`
- **方式**：POST
- **说明**：重新生成桌台的二维码
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：tableId：桌台ID
- **响应**：新生成的二维码URL

##### 7.6.2.9 强制结束桌台使用

- **路径**：`/api/admin/tables/{tableId}/end-usage`
- **方式**：POST
- **说明**：强制结束当前桌台的使用订单
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：tableId：桌台ID
- **请求参数**：
  - reason：结束原因
- **响应**：操作结果，包含结束的订单信息

#### 7.6.3 计费规则管理接口

##### 7.6.3.1 创建计费规则

- **路径**：`/api/admin/price-rules`
- **方式**：POST
- **说明**：创建新的计费规则
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - merchantId：所属商家ID
  - name：规则名称
  - ruleType：规则类型（1-标准计费 2-阶梯计费）
  - priceUnit：单价（标准计费时必填）
  - memberPrice：会员价格（可选）
  - minMinutes：最低消费时长（可选）
  - maxPrice：封顶价格（可选）
  - ladderRules：阶梯计费规则JSON（阶梯计费时必填）
- **响应**：新创建的计费规则信息

##### 7.6.3.2 更新计费规则

- **路径**：`/api/admin/price-rules/{ruleId}`
- **方式**：PUT
- **说明**：更新计费规则信息
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：ruleId：规则ID
- **请求参数**：与创建计费规则参数相同，但所有字段都是可选的
- **响应**：更新后的计费规则信息

##### 7.6.3.3 删除计费规则

- **路径**：`/api/admin/price-rules/{ruleId}`
- **方式**：DELETE
- **说明**：删除指定计费规则
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：ruleId：规则ID
- **响应**：操作结果

##### 7.6.3.4 获取计费规则列表

- **路径**：`/api/admin/price-rules`
- **方式**：GET
- **说明**：获取商家所有计费规则
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - merchantId：商家ID
  - status：状态筛选
  - type：规则类型筛选
  - keyword：关键词搜索
  - pageNum：页码，默认1
  - pageSize：每页数量，默认10
- **响应**：计费规则列表分页数据

##### 7.6.3.5 获取计费规则详情

- **路径**：`/api/admin/price-rules/{ruleId}`
- **方式**：GET
- **说明**：获取指定计费规则详情
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：ruleId：规则ID
- **响应**：计费规则详细信息

##### 7.6.3.6 应用计费规则到桌台

- **路径**：`/api/admin/price-rules/{ruleId}/apply`
- **方式**：POST
- **说明**：将计费规则应用到指定桌台
- **请求头**：Authorization: Bearer {adminToken}
- **路径参数**：ruleId：规则ID
- **请求参数**：
  - tableIds：桌台ID数组
- **响应**：操作结果

##### 7.6.3.7 计费规则预览测算

- **路径**：`/api/admin/price-rules/preview`
- **方式**：POST
- **说明**：预览计算指定时长的费用
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - ruleId：规则ID或规则详情对象
  - minutes：使用时长（分钟）
  - isMember：是否会员（可选，默认false）
- **响应**：预计费用金额和计算明细

### 7.7 仪表盘接口

#### 7.7.1 获取仪表盘概览数据

- **路径**：`/api/admin/dashboard/overview`
- **方式**：GET
- **说明**：获取仪表盘概览数据，包含关键业务指标
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - timeRange：统计时间范围，支持today/week/month/custom
  - startDate：自定义时间范围的开始日期（timeRange=custom时必填）
  - endDate：自定义时间范围的结束日期（timeRange=custom时必填）
  - merchantId：商家ID
  - storeIds：门店ID数组，可多选
- **响应**：
  ```json
  {
    "code": 0,
    "msg": "success",
    "data": {
      "timeRange": "today",
      "statistics": {
        "totalRevenue": 12580.5,       // 总营收
        "orderCount": 156,             // 订单总数
        "customerCount": 132,          // 客户数
        "tableUsageRate": 0.75,        // 桌台利用率
        "avgOrderAmount": 80.64,       // 平均订单金额
        "avgUseTime": 85,              // 平均使用时长(分钟)
        "memberOrderRate": 0.68        // 会员订单占比
      },
      "comparison": {
        "revenueGrowth": 0.12,         // 营收环比增长
        "orderCountGrowth": 0.05,      // 订单数环比增长
        "customerCountGrowth": 0.08,   // 客户数环比增长
        "tableUsageGrowth": -0.03      // 桌台利用率环比变化
      },
      "realtime": {
        "currentActiveOrders": 28,     // 当前进行中订单数
        "currentRevenue": 3650.5,      // 当前已产生营收
        "predictedRevenue": 8500.0,    // 今日预计总营收
        "activeTableCount": 35,        // 当前使用中桌台数
        "totalTableCount": 48,         // 总桌台数
        "updateTime": "2023-04-20 15:30:45" // 数据更新时间
      }
    }
  }
  ```

#### 7.7.2 获取门店营收排行

- **路径**：`/api/admin/dashboard/store-ranking`
- **方式**：GET
- **说明**：获取门店营收排行数据
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - timeRange：统计时间范围，支持today/week/month/custom
  - startDate：自定义时间范围的开始日期（可选）
  - endDate：自定义时间范围的结束日期（可选）
  - merchantId：商家ID
  - sortBy：排序字段，支持revenue/orderCount/tableUsage/avgOrder
  - order：排序方式，asc或desc，默认desc
  - limit：返回数量限制，默认10
- **响应**：
  ```json
  {
    "code": 0,
    "msg": "success",
    "data": [
      {
        "storeId": "store_001",
        "storeName": "星辉台球城旗舰店",
        "revenue": 35680.5,
        "orderCount": 320,
        "tableUsageRate": 0.82,
        "avgOrderAmount": 111.5,
        "memberRate": 0.75,
        "growth": 0.15
      },
      // ... 其他门店数据
    ]
  }
  ```

#### 7.7.3 获取时段分析数据

- **路径**：`/api/admin/dashboard/hourly-analysis`
- **方式**：GET
- **说明**：获取按小时分组的业务数据
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - date：分析日期，默认今日
  - storeId：门店ID（可选，不传则统计所有门店）
  - merchantId：商家ID
  - metrics：需要返回的指标，可选值为revenue/orderCount/tableUsage，多选用逗号分隔
- **响应**：
  ```json
  {
    "code": 0,
    "msg": "success",
    "data": {
      "date": "2023-04-20",
      "hourlyData": [
        {
          "hour": 0,
          "revenue": 1250.5,
          "orderCount": 8,
          "tableUsage": 0.45
        },
        // ... 24小时数据
      ],
      "peakHours": {
        "revenue": [20, 21, 22],    // 营收高峰时段
        "orderCount": [19, 20, 21], // 订单高峰时段
        "tableUsage": [20, 21, 22]  // 桌台使用率高峰时段
      }
    }
  }
  ```

#### 7.7.4 获取异常订单统计

- **路径**：`/api/admin/dashboard/abnormal-orders`
- **方式**：GET
- **说明**：获取需要关注的异常订单
- **请求头**：Authorization: Bearer {adminToken}
- **请求参数**：
  - merchantId：商家ID
  - storeIds：门店ID数组（可选）
  - type：异常类型，支持all/longTime/paymentFailed/refunding/disputed
- **响应**：
  ```json
  {
    "code": 0,
    "msg": "success",
    "data": {
      "total": 8,
      "abnormalOrders": [
        {
          "orderId": "order_12345",
          "orderNo": "SO2023042001258",
          "storeId": "store_001",
          "storeName": "星辉台球城旗舰店",
          "tableId": "table_035",
          "tableNumber": "A-12",
          "abnormalType": "longTime",    // 异常类型
          "abnormalReason": "使用时长超过5小时",  // 异常原因
          "startTime": "2023-04-20 09:30:25",
          "currentAmount": 280.5,
          "userId": "user_8976",
          "userName": "张三",
          "status": 0  // 订单状态
        },
        // ... 其他异常订单
      ]
    }
  }
  ```

### 7.8 用户管理接口

#### 7.8.1 获取用户列表

- **路径**: `/api/admin/users`
- **方式**: GET
- **说明**: 获取用户列表，支持分页和筛选
- **请求头**: Authorization: Bearer {adminToken}
- **请求参数**:
  - pageNum: 页码，默认1
  - pageSize: 每页数量，默认20
  - keyword: 搜索关键词（匹配用户名、手机号等）
  - memberLevel: 会员等级筛选（可选）
  - registerTimeStart: 注册时间开始（可选）
  - registerTimeEnd: 注册时间结束（可选）
  - sortBy: 排序字段，如registerTime/lastLoginTime/points
  - sortOrder: 排序方向，asc/desc
- **响应**:
  ```json
  {
    "code": 0,
    "msg": "success",
    "data": {
      "total": 1580,
      "pages": 79,
      "pageNum": 1,
      "pageSize": 20,
      "list": [
        {
          "id": "user_12345",
          "nickname": "小明",
          "avatar": "https://example.com/avatar.jpg",
          "phone": "13812345678",
          "gender": 1,
          "isMember": true,
          "memberLevel": 2,
          "memberExpire": "2023-12-31 23:59:59",
          "points": 2800,
          "createTime": "2023-01-15 14:30:22",
          "lastLoginTime": "2023-04-19 19:22:35",
          "status": 0,
          "totalConsumption": 1580.5,
          "consumptionCount": 25
        },
        // ...其他用户
      ]
    }
  }
  ```

#### 7.8.2 获取用户详情

- **路径**: `/api/admin/users/{userId}`
- **方式**: GET
- **说明**: 获取用户详细信息
- **请求头**: Authorization: Bearer {adminToken}
- **路径参数**: userId: 用户ID
- **响应**:
  ```json
  {
    "code": 0,
    "msg": "success",
    "data": {
      "basicInfo": {
        "id": "user_12345",
        "nickname": "小明",
        "avatar": "https://example.com/avatar.jpg",
        "phone": "13812345678",
        "gender": 1,
        "createTime": "2023-01-15 14:30:22",
        "lastLoginTime": "2023-04-19 19:22:35",
        "status": 0
      },
      "memberInfo": {
        "isMember": true,
        "memberLevel": 2,
        "memberLevelName": "银牌会员",
        "memberExpire": "2023-12-31 23:59:59",
        "points": 2800,
        "nextLevelPoints": 5000,
        "nextLevelName": "金牌会员"
      },
      "consumptionInfo": {
        "totalConsumption": 1580.5,
        "consumptionCount": 25,
        "avgConsumption": 63.22,
        "favoriteStoreId": "store_001",
        "favoriteStoreName": "星辉台球城旗舰店",
        "avgUseTime": 78, // 分钟
        "lastConsumptionTime": "2023-04-19 19:22:35"
      }
    }
  }
  ```

#### 7.8.3 更新用户会员信息

- **路径**: `/api/admin/users/{userId}/member`
- **方式**: PUT
- **说明**: 更新用户的会员信息
- **请求头**: Authorization: Bearer {adminToken}
- **路径参数**: userId: 用户ID
- **请求参数**:
  ```json
  {
    "memberLevel": 3,
    "memberExpire": "2024-04-30 23:59:59",
    "points": 5000,
    "updateReason": "营销活动升级"
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "msg": "success",
    "data": {
      "userId": "user_12345",
      "memberLevel": 3,
      "memberExpire": "2024-04-30 23:59:59",
      "points": 5000,
      "updateTime": "2023-04-20 15:30:22",
      "operatorId": "admin_001"
    }
  }
  ```

#### 7.8.4 获取用户消费记录

- **路径**: `/api/admin/users/{userId}/orders`
- **方式**: GET
- **说明**: 获取用户的消费订单记录
- **请求头**: Authorization: Bearer {adminToken}
- **路径参数**: userId: 用户ID
- **请求参数**:
  - pageNum: 页码，默认1
  - pageSize: 每页数量，默认20
  - status: 订单状态筛选（可选）
  - timeStart: 时间范围开始（可选）
  - timeEnd: 时间范围结束（可选）
- **响应**:
  ```json
  {
    "code": 0,
    "msg": "success",
    "data": {
      "total": 25,
      "pages": 2,
      "pageNum": 1,
      "pageSize": 20,
      "list": [
        {
          "orderId": "order_67890",
          "orderNo": "SO2023041900358",
          "storeId": "store_001",
          "storeName": "星辉台球城旗舰店",
          "tableId": "table_008",
          "tableNumber": "A-8",
          "startTime": "2023-04-19 19:22:35",
          "endTime": "2023-04-19 21:35:12",
          "duration": 133, // 分钟
          "amount": 85.5,
          "status": 1 // 已完成
        },
        // ... 其他订单
      ]
    }
  }
  ```

## 8. 数据库设计（补充）

### 8.1 数据表设计

#### 8.1.8 会员等级表(member_level)

| 字段名          | 数据类型      | 说明                       | 约束              |
|-----------------|--------------|---------------------------|------------------|
| id              | varchar(32)  | 等级ID                    | 主键              |
| merchant_id     | varchar(32)  | 所属商家ID                 | 外键, 非空         |
| level           | tinyint(2)   | 等级数值                    | 非空              |
| name            | varchar(32)  | 等级名称                    | 非空              |
| icon            | varchar(255) | 等级图标URL                 | 可空              |
| min_points      | int          | 等级最低积分要求              | 非空              |
| discount        | decimal(3,2) | 折扣率(0.1-1.0)            | 非空              |
| special_discount | decimal(3,2) | 特殊时段折扣率               | 可空              |
| description     | varchar(255) | 等级说明                    | 可空              |
| create_time     | datetime     | 创建时间                    | 非空              |
| update_time     | datetime     | 更新时间                    | 非空              |
| status          | tinyint(1)   | 状态 0-正常 1-禁用           | 默认0             |

#### 8.1.9 用户积分记录表(user_points_log)

| 字段名          | 数据类型      | 说明                       | 约束              |
|-----------------|--------------|---------------------------|------------------|
| id              | varchar(32)  | 记录ID                    | 主键              |
| user_id         | varchar(32)  | 用户ID                    | 外键, 非空         |
| merchant_id     | varchar(32)  | 所属商家ID                 | 外键, 非空         |
| points          | int          | 积分变动数量                | 非空              |
| points_type     | tinyint(1)   | 类型 1-增加 2-减少           | 非空              |
| source          | tinyint(2)   | 来源 1-消费 2-活动 3-签到 4-兑换 | 非空           |
| order_id        | varchar(32)  | 关联订单ID                 | 可空              |
| description     | varchar(128) | 变动说明                    | 非空              |
| before_points   | int          | 变动前积分                  | 非空              |
| after_points    | int          | 变动后积分                  | 非空              |
| create_time     | datetime     | 创建时间                    | 非空              |
| operator_id     | varchar(32)  | 操作人ID                   | 可空              |

#### 8.1.10 优惠券表(coupon)

| 字段名          | 数据类型      | 说明                       | 约束              |
|-----------------|--------------|---------------------------|------------------|
| id              | varchar(32)  | 优惠券ID                   | 主键              |
| merchant_id     | varchar(32)  | 所属商家ID                 | 外键, 非空         |
| name            | varchar(64)  | 优惠券名称                  | 非空              |
| type            | tinyint(2)   | 类型 1-折扣券 2-满减券 3-时长券 | 非空            |
| discount        | decimal(3,2) | 折扣率(type=1时使用)        | 可空              |
| amount          | decimal(10,2)| 优惠金额(type=2时使用)      | 可空              |
| min_amount      | decimal(10,2)| 最低消费金额(type=2时使用)   | 可空              |
| minutes         | int          | 赠送时长(type=3时使用)      | 可空              |
| valid_days      | int          | 有效期天数                  | 非空              |
| use_condition   | varchar(255) | 使用条件说明                | 可空              |
| create_time     | datetime     | 创建时间                    | 非空              |
| update_time     | datetime     | 更新时间                    | 非空              |
| status          | tinyint(1)   | 状态 0-正常 1-已停用         | 默认0             |

#### 8.1.11 用户优惠券表(user_coupon)

| 字段名          | 数据类型      | 说明                       | 约束              |
|-----------------|--------------|---------------------------|------------------|
| id              | varchar(32)  | 用户优惠券ID                | 主键              |
| user_id         | varchar(32)  | 用户ID                    | 外键, 非空         |
| coupon_id       | varchar(32)  | 优惠券ID                   | 外键, 非空         |
| source          | tinyint(2)   | 来源 1-系统发放 2-活动获取 3-积分兑换 | 非空         |
| get_time        | datetime     | 领取时间                    | 非空              |
| start_time      | datetime     | 生效时间                    | 非空              |
| end_time        | datetime     | 过期时间                    | 非空              |
| use_time        | datetime     | 使用时间                    | 可空              |
| order_id        | varchar(32)  | 使用订单ID                  | 可空              |
| status          | tinyint(1)   | 状态 0-未使用 1-已使用 2-已过期 | 默认0            |

#### 8.1.12 用户余额表(user_balance)

| 字段名          | 数据类型      | 说明                       | 约束              |
|-----------------|--------------|---------------------------|------------------|
| id              | varchar(32)  | 记录ID                    | 主键              |
| user_id         | varchar(32)  | 用户ID                    | 外键, 非空         |
| merchant_id     | varchar(32)  | 所属商家ID                 | 外键, 非空         |
| balance         | decimal(10,2)| 当前余额                    | 非空, 默认0       |
| frozen_balance  | decimal(10,2)| 冻结余额                    | 非空, 默认0       |
| create_time     | datetime     | 创建时间                    | 非空              |
| update_time     | datetime     | 更新时间                    | 非空              |
| status          | tinyint(1)   | 状态 0-正常 1-冻结           | 默认0             |

## 9. 未来功能规划

### 9.1 优惠券系统

#### 9.1.1 功能概述
优惠券系统旨在增强营销能力，提高用户活跃度和复购率，通过多样化的优惠形式刺激消费，提升客户满意度。

#### 9.1.2 核心功能规划
- **优惠券类型**：
  - 折扣券：基于消费金额的比例折扣
  - 满减券：满足一定消费金额后的固定金额减免
  - 时长券：赠送特定使用时长
  - 组合券：多种优惠形式组合
  
- **发放渠道**：
  - 新用户注册自动发放
  - 会员等级升级奖励
  - 定期活动发放
  - 节假日特别优惠
  - 客户关怀（生日、纪念日）
  
- **优惠券管理**：
  - 批量创建与发放功能
  - 优惠券使用数据统计
  - 优惠券有效性管理
  - 使用限制条件设置

#### 9.1.3 预期价值
- 提高用户首次消费转化率
- 增加会员复购频次
- 淡季促销工具
- 提升用户满意度和忠诚度

### 9.2 用户评价系统

#### 9.2.1 功能概述
用户评价系统允许用户对使用体验进行评分和评论，帮助商家了解客户满意度，发现服务问题，并为潜在客户提供参考。

#### 9.2.2 核心功能规划
- **评分维度**：
  - 总体满意度（星级评分）
  - 环境评分
  - 设备评分
  - 服务评分
  - 价格合理性评分
  
- **评价内容**：
  - 文字评论
  - 标签化评价（快速选择标签）
  - 图片上传
  - 视频评价（可选）
  
- **评价管理**：
  - 商家回复功能
  - 优质评价展示
  - 问题评价处理流程
  - 评价数据分析报表

#### 9.2.3 预期价值
- 收集真实用户反馈，改进服务
- 通过评价展示增强社会证明
- 发现并及时处理客户投诉
- 优质评价作为营销素材

### 9.3 报表系统

#### 9.3.1 功能概述
报表系统提供更丰富的数据分析和可视化功能，帮助商家洞察业务趋势，制定经营决策，并支持多维度的数据导出和分享。

#### 9.3.2 核心功能规划
- **预设报表模板**：
  - 日/周/月经营概况报表
  - 会员增长与活跃分析
  - 桌台使用率分析
  - 营销活动效果分析
  - 客户画像分析
  
- **自定义报表**：
  - 拖拽式报表设计器
  - 指标自由组合
  - 条件筛选设置
  - 图表类型自定义
  
- **报表调度**：
  - 定时生成报表
  - 报表邮件推送
  - 报表权限管理
  - 报表历史版本管理

#### 9.3.3 预期价值
- 辅助商家做出数据驱动的决策
- 自动化报表生成，减少人工工作
- 提供多角度业务分析视角
- 支持商业计划的制定与评估

### 9.4 一键开台功能

#### 9.4.1 功能概述
一键开台功能通过简化用户开台流程，提升用户体验，特别为老用户和高频用户提供便捷服务，减少操作步骤。

#### 9.4.2 核心功能规划
- **智能开台**：
  - 基于历史偏好推荐桌台
  - 一键开启最近一次使用的桌台类型
  - 预设开台时长选择
  
- **快速支付**：
  - 记住用户支付偏好
  - 快速完成预付费充值
  - 简化结算流程
  
- **团队开台**：
  - 多人分摊费用
  - 好友共同开台邀请
  - 团队使用权限管理

#### 9.4.3 预期价值
- 提升用户使用便捷性
- 减少开台环节的流失率
- 增强用户粘性和忠诚度
- 促进社交传播和新客获取

### 9.5 社交功能

#### 9.5.1 功能概述
社交功能旨在构建台球爱好者社区，增强用户间互动，提升平台粘性，并通过用户自传播扩大获客渠道。

#### 9.5.2 核心功能规划
- **球友社区**：
  - 用户动态发布与分享
  - 技术交流区
  - 球友排行榜
  - 兴趣小组
  
- **约球系统**：
  - 发布约球邀请
  - 匹配相近水平球友
  - 预约场地和时间
  - 结束后互评系统
  
- **比赛系统**：
  - 线上赛事报名
  - 赛程管理与公示
  - 比赛结果记录
  - 奖励体系

#### 9.5.3 预期价值
- 构建垂直领域社区生态
- 通过社交连接提高用户留存
- 降低获客成本，提高品牌曝光
- 丰富产品功能，差异化竞争

